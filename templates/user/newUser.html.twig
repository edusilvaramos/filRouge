{% extends 'base.html.twig' %}

{% block title %}New User
{% endblock %}

{% block body %}
	<div class="container mt-5 mb-5 justify-content-center ">
		<h1 class="text-center text-primary mb-4 ">Create New User</h1>
		<div class="col-lg-12 col-md-8 col-sm-12 justify-content-center">
			<div class="card shadow-sm p-4 justify-content-center">

				{{ component('CheckForm') }}
			{# {{ include('user/formUser.html.twig') }} #}
			</div>
		</div>

		<div class="text-center mt-4">
			<a href="{{ path('app_user_index') }}" class="btn btn-outline-secondary btn-lg" aria-label="Back to user list">
				<i class="bi bi-arrow-left-circle"></i>
				Back to list
			</a>
		</div>
	</div>
{% endblock %}
