{% extends 'base.html.twig' %}


{% block body %}
	<div class="container mt-5 w-75 fs-3">
		<div class=" d-flex justify-content-between mt-5 mb-4 w-75 ms-5">
			<img src="{{ asset('' ~ app.user.photoUser) }}" alt="User Photo" class="img-fluid rounded-circle w-25 p-3">
			<h1 class="text-success mx-auto ms-5 mt-5 fs-2">
				{{ app.user.firstName }}
				{{ app.user.lastName }}
			</h1>
		</div>
		<hr class="border border-danger rounded-1 border-2 opacity-100 mt-1 mb-2"/>

		<ul>
			<li><a href="{{path('app_user_index')}}">Mes cordonnees</a></li>
			<li><a href="{{path('app_address_index')}}">Mes adresses</a></li>
		</ul>
	</div>

{% endblock %}
