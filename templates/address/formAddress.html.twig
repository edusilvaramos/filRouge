<div class="container mt-5 mb-5 w-50-lg w-50-lg w-100">
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<hr class="border border-danger rounded-1 border-2 opacity-100 mt-1 mb-2"/>

	<button class="btn btn-primary mt-3">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}
</div>
