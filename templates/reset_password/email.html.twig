<h1>Salut, {{ user.username }}</h1>

<p>Pour actualiser votre mot de passe, veuillez cliquer sur le lien suivant</p>

<button class="btn btn-primary">
<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>
</button>

<p>Ce lien expirera {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Si vous n'avez pas demandé ce changement de mot de passe, merci de ne pas le faire.</p>
